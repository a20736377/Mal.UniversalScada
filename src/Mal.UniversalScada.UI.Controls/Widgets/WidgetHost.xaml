<UserControl x:Class="Mal.UniversalScada.UI.Controls.Widgets.WidgetHost"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:Mal.UniversalScada.UI.Controls.ViewModels"
             xmlns:widgets="clr-namespace:Mal.UniversalScada.UI.Controls.Widgets"
             xmlns:models="clr-namespace:Mal.UniversalScada.Core.Models;assembly=Mal.UniversalScada.Core"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance vm:WidgetViewModel}"
             Canvas.Left="{Binding X, Mode=TwoWay}"
             Canvas.Top="{Binding Y, Mode=TwoWay}"
             Width="{Binding Width, Mode=TwoWay}"
             Height="{Binding Height, Mode=TwoWay}">
    
    <UserControl.Style>
        <Style TargetType="UserControl">
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsDesignMode}" Value="True">
                    <Setter Property="Cursor" Value="SizeAll"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </UserControl.Style>

    <Grid>
        <!-- 核心真实可视化控件 (根据 WidgetType 切换显示) -->
        <widgets:CircularGaugeControl>
            <widgets:CircularGaugeControl.Style>
                <Style TargetType="widgets:CircularGaugeControl">
                    <Setter Property="Visibility" Value="Collapsed"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding Type}" Value="{x:Static models:WidgetType.GaugeCircular}">
                            <Setter Property="Visibility" Value="Visible"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </widgets:CircularGaugeControl.Style>
        </widgets:CircularGaugeControl>

        <widgets:TankLevelControl>
            <widgets:TankLevelControl.Style>
                <Style TargetType="widgets:TankLevelControl">
                    <Setter Property="Visibility" Value="Collapsed"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding Type}" Value="{x:Static models:WidgetType.LevelTank}">
                            <Setter Property="Visibility" Value="Visible"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </widgets:TankLevelControl.Style>
        </widgets:TankLevelControl>

        <widgets:NumericCardControl>
            <widgets:NumericCardControl.Style>
                <Style TargetType="widgets:NumericCardControl">
                    <Setter Property="Visibility" Value="Collapsed"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding Type}" Value="{x:Static models:WidgetType.NumericCard}">
                            <Setter Property="Visibility" Value="Visible"/>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding Type}" Value="{x:Static models:WidgetType.TextLabel}">
                            <Setter Property="Visibility" Value="Visible"/>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding Type}" Value="{x:Static models:WidgetType.SetpointInput}">
                            <Setter Property="Visibility" Value="Visible"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </widgets:NumericCardControl.Style>
        </widgets:NumericCardControl>

        <widgets:IoMatrixControl>
            <widgets:IoMatrixControl.Style>
                <Style TargetType="widgets:IoMatrixControl">
                    <Setter Property="Visibility" Value="Collapsed"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding Type}" Value="{x:Static models:WidgetType.IoMatrix}">
                            <Setter Property="Visibility" Value="Visible"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </widgets:IoMatrixControl.Style>
        </widgets:IoMatrixControl>

        <widgets:StatusLedControl>
            <widgets:StatusLedControl.Style>
                <Style TargetType="widgets:StatusLedControl">
                    <Setter Property="Visibility" Value="Collapsed"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding Type}" Value="{x:Static models:WidgetType.StatusLed}">
                            <Setter Property="Visibility" Value="Visible"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </widgets:StatusLedControl.Style>
        </widgets:StatusLedControl>

        <widgets:ControlButtonControl>
            <widgets:ControlButtonControl.Style>
                <Style TargetType="widgets:ControlButtonControl">
                    <Setter Property="Visibility" Value="Collapsed"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding Type}" Value="{x:Static models:WidgetType.ControlButton}">
                            <Setter Property="Visibility" Value="Visible"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </widgets:ControlButtonControl.Style>
        </widgets:ControlButtonControl>

        <!-- 仅设计模式显示的顶部拖拽手柄条 -->
        <Border Height="18" VerticalAlignment="Top" Background="#1E293B" CornerRadius="8,8,0,0" Opacity="0.85"
                IsHitTestVisible="False">
            <Border.Style>
                <Style TargetType="Border">
                    <Setter Property="Visibility" Value="Collapsed"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsDesignMode}" Value="True">
                            <Setter Property="Visibility" Value="Visible"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Border.Style>
            <DockPanel Margin="6,0">
                <TextBlock Text="⠿" FontSize="11" Foreground="#38BDF8" FontWeight="Bold" VerticalAlignment="Center"/>
                <TextBlock Text="{Binding Title}" FontSize="9" Foreground="#94A3B8" VerticalAlignment="Center" Margin="4,0,0,0" TextTrimming="CharacterEllipsis"/>
            </DockPanel>
        </Border>

        <!-- 设计模式专属：高亮选中框与拖拽遮罩 -->
        <Border x:Name="DesignerAdorner"
                BorderThickness="2" CornerRadius="10" IsHitTestVisible="False">
            <Border.Style>
                <Style TargetType="Border">
                    <Setter Property="Visibility" Value="Collapsed"/>
                    <Setter Property="BorderBrush" Value="Transparent"/>
                    <Style.Triggers>
                        <MultiDataTrigger>
                            <MultiDataTrigger.Conditions>
                                <Condition Binding="{Binding IsDesignMode}" Value="True"/>
                                <Condition Binding="{Binding IsSelected}" Value="True"/>
                            </MultiDataTrigger.Conditions>
                            <Setter Property="Visibility" Value="Visible"/>
                            <Setter Property="BorderBrush" Value="#38BDF8"/>
                        </MultiDataTrigger>
                    </Style.Triggers>
                </Style>
            </Border.Style>
        </Border>

        <!-- 选中状态右上角删除按钮 (仅设计模式显示) -->
        <Button x:Name="DeleteButton" Content="✕" Width="20" Height="20" HorizontalAlignment="Right" VerticalAlignment="Top"
                Margin="0,-8,-8,0" Background="#EF4444" Foreground="#FFFFFF" FontWeight="Bold" FontSize="11"
                BorderThickness="0" Click="OnDeleteWidgetClick" ToolTip="删除当前组件">
            <Button.Resources>
                <Style TargetType="Border">
                    <Setter Property="CornerRadius" Value="10"/>
                </Style>
            </Button.Resources>
            <Button.Style>
                <Style TargetType="Button">
                    <Setter Property="Visibility" Value="Collapsed"/>
                    <Style.Triggers>
                        <MultiDataTrigger>
                            <MultiDataTrigger.Conditions>
                                <Condition Binding="{Binding IsDesignMode}" Value="True"/>
                                <Condition Binding="{Binding IsSelected}" Value="True"/>
                            </MultiDataTrigger.Conditions>
                            <Setter Property="Visibility" Value="Visible"/>
                        </MultiDataTrigger>
                    </Style.Triggers>
                </Style>
            </Button.Style>
        </Button>
    </Grid>
</UserControl>
