<Window x:Class="S7Tester.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:S7Tester"
        mc:Ignorable="d"
        Title="S7 连接测试工具" Height="560" Width="900"
        WindowStartupLocation="CenterScreen"
        d:DataContext="{d:DesignInstance Type=vm:MainViewModel}">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 参数输入区 -->
        <GroupBox Grid.Row="0" Header="连接参数" Padding="8" Margin="0,0,0,8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="160"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="80"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="80"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="140"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Column="0" Text="IP 地址:" VerticalAlignment="Center" Margin="0,0,6,0"/>
                <TextBox   Grid.Column="1" Text="{Binding Ip, UpdateSourceTrigger=PropertyChanged}" VerticalAlignment="Center"/>

                <TextBlock Grid.Column="2" Text="机架号:" VerticalAlignment="Center" Margin="12,0,6,0"/>
                <TextBox   Grid.Column="3" Text="{Binding Rack, UpdateSourceTrigger=PropertyChanged}" VerticalAlignment="Center"/>

                <TextBlock Grid.Column="4" Text="插槽号:" VerticalAlignment="Center" Margin="12,0,6,0"/>
                <TextBox   Grid.Column="5" Text="{Binding Slot, UpdateSourceTrigger=PropertyChanged}" VerticalAlignment="Center"/>

                <TextBlock Grid.Column="6" Text="CPU 类型:" VerticalAlignment="Center" Margin="12,0,6,0"/>
                <ComboBox  Grid.Column="7" ItemsSource="{Binding CpuTypes}"
                           SelectedItem="{Binding SelectedCpuType}" VerticalAlignment="Center"/>
            </Grid>
        </GroupBox>

        <!-- 操作按钮 + 状态 -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,8">
            <Button Content="连接测试" Command="{Binding ConnectCommand}" Padding="16,4" Margin="0,0,8,0"/>
            <Button Content="读取数据" Command="{Binding ReadCommand}" Padding="16,4" Margin="0,0,8,0"/>
            <Button Content="断开"     Command="{Binding DisconnectCommand}" Padding="16,4" Margin="0,0,16,0"/>
            <TextBlock Text="{Binding StatusMessage}" VerticalAlignment="Center" FontWeight="Bold"/>
        </StackPanel>

        <!-- 地址输入 + 结果表 -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="260"/>
                <ColumnDefinition Width="6"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <DockPanel Grid.Column="0">
                <TextBlock DockPanel.Dock="Top" Text="点位地址（每行一个）：" Margin="0,0,0,4"/>
                <TextBox Text="{Binding Addresses, UpdateSourceTrigger=PropertyChanged}"
                         AcceptsReturn="True" VerticalScrollBarVisibility="Auto"
                         FontFamily="Consolas" FontSize="13"
                         TextWrapping="NoWrap" VerticalAlignment="Stretch"/>
            </DockPanel>

            <GridSplitter Grid.Column="1" Width="6" HorizontalAlignment="Stretch"/>

            <DataGrid Grid.Column="2"
                      ItemsSource="{Binding Results}"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      HeadersVisibility="Column"
                      GridLinesVisibility="All"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="地址"   Binding="{Binding Address}"   Width="140"/>
                    <DataGridTextColumn Header="值"     Binding="{Binding Value}"     Width="*"/>
                    <DataGridTextColumn Header="类型"   Binding="{Binding RawType}"   Width="100"/>
                    <DataGridTextColumn Header="质量"   Binding="{Binding Quality}"  Width="60"/>
                    <DataGridTextColumn Header="时间戳" Binding="{Binding Timestamp}" Width="110"/>
                    <DataGridTextColumn Header="错误"   Binding="{Binding Error}"     Width="200"/>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
    </Grid>
</Window>
